/**
 * Created by johan on 2/24/2017.
 */
import messenger from './socket';
/*import {addObject, updateObject} from './redux/actions_viz'

class ObjectVizTracker{
    constructor(){
        this.objects = [];
        this.creatorchannel = messenger.addChannel({
            name: 'newobject',
            onrecieve: function(createdobj){
                store.dispatch(addObject(createdobj.classname, createdobj.name, createdobj.state))
            }
        });
        this.updaterchannel = messenger.addChannel({
            name: 'updateobject',
            onrecieve: function(updatedobj){
                store.dispatch(updateObject(updatedobj.name, updatedobj.state))
            }
        });
    }

    createObject(classname, name, args){
        creatorchannel.send({
            classname, name, args
        });
    }
}

messenger.addChannel({
    name: 'newobject',
});

export const objviz = new ObjectVizTracker();*/


